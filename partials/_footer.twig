
{% setcontent settings = "settings" limit 1 returnsingle %}
<footer class="footer">
    <div>
        <div style="border-top: 1px solid black;">
            <span class="small">© {{ "now"|date("Y") }} {{ settings.footer }}</span>
        </div>
    </div>
    {% if settings.socialmedia != "" %}
    <div>
        {% for item in settings.socialmedia %}
            <a href="{{item.links}}"><div class="button">{{item.title|first}}</div></a>
        {% endfor %}
    </div>
    {% endif %}
</footer>
    
    <!-- Assets -->
    <script src={{ asset('js/jquery-3.3.1.slim.min.js', 'theme') }}></script>
    <script src={{ asset('js/bootstrap.min.js', 'theme') }}></script>
    <script src={{ asset('js/flexfolio.js', 'theme') }}></script>

    <!--<div id="spinner" class="loading">Loading&#8230;</div>-->

    <!-- Cookie-Alert -->
    <div class="alert text-center cookiealert" role="alert">
        Cookies used to ensure you get the best experience.
        <button type="button" class="button acceptcookies" aria-label="Close">
            OK
        </button>
    </div>
    <script src="{{ asset('js/cookiealert.js', 'theme') }}"></script>

  </body>
</html>
