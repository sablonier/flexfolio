<nav class="navbar navbar-toggleable-md navbar-light fixed-top bg-faded">
  
  <!-- Sitename as home button -->
  {% set homebutton = config.get('general/sitename') %}
  <div style="white-space: nowrap;">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" style="padding-left: 0;" href="{{ '/' }}">
          {{ homebutton }}</a>
      </li>
    </ul>
  </div>

  <!-- Responsive Toggle -->
  <button aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation"
    class="navbar-toggler navbar-toggler-right" data-target="#navbarNavDropdown" data-toggle="collapse" type="button">
    <span class="navbar-toggler-icon"></span>
  </button>

  <!-- Menu -->
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    {{ include('partials/_menu.twig') }}

    <!-- Search -->
    {# <form class="form-inline my-2 my-lg-0" method="get" action="{{ path('search') }}" id="searchform-inline" enctype="text/plain">
                <div class="input-group">
                    <input type="search" class="form-control" value="{% if search is defined %}{{ search|escape }}{% endif %}" placeholder="{{ __('general.phrase.search-ellipsis') }}" name="search">
                    <span class="input-group-btn">
                    <button class="btn btn-primary" type="submit">{{ __('general.phrase.search') }}</button>
                    </span>
                </div>
                </form>
              </div>#}
</nav>